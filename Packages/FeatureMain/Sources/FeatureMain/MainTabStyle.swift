import DesignSystem
import SwiftUI

enum MainTabStyle {
    static let centerButtonSize: CGFloat = 68
    static let centerButtonGradient = LinearGradient(
        colors: [AppColor.accent, AppColor.brand],
        startPoint: .topLeading,
        endPoint: .bottomTrailing
    )
}

