import DesignSystem
import SwiftUI

enum MoreStyle {
    static let profileGradient = LinearGradient(
        colors: [AppColor.brandDark, AppColor.brand],
        startPoint: .leading,
        endPoint: .trailing
    )
    static let menuIconSize: CGFloat = 42
}

